<ContentDialog
    x:Class="EDAccountSwitcher.Updating.UpdateDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:EDAccountSwitcher.Localization"
    Title="{loc:Loc Key=Update_Title}">

    <StackPanel Width="420" Spacing="12">
        <TextBlock x:Name="VersionsText" FontWeight="SemiBold" />
        <TextBlock x:Name="SizeText" FontSize="12" Opacity="0.7" />

        <TextBlock x:Name="WhatsNewHeader" FontWeight="SemiBold" Margin="0,4,0,0"
                   Text="{loc:Loc Key=Update_WhatsNew}" />

        <Border CornerRadius="8" Padding="12" MaxHeight="220"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="NotesHost" Spacing="6" />
            </ScrollViewer>
        </Border>

        <ProgressBar x:Name="Progress" Visibility="Collapsed" />
        <TextBlock x:Name="StatusText" Visibility="Collapsed" FontSize="12" Opacity="0.7"
                   TextWrapping="Wrap" />

        <StackPanel x:Name="ActionRow" Orientation="Horizontal" Spacing="8"
                    HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button x:Name="SkipButton" Content="{loc:Loc Key=Update_Skip}"
                    CornerRadius="16" MinWidth="0" Padding="16,6" Height="32"
                    Click="OnSkipClick" />
            <Button x:Name="LaterButton" Content="{loc:Loc Key=Update_Later}"
                    CornerRadius="16" MinWidth="0" Padding="16,6" Height="32"
                    Click="OnLaterClick" />
            <Button x:Name="UpdateButton" Content="{loc:Loc Key=Update_Install}"
                    CornerRadius="16" MinWidth="0" Padding="16,6" Height="32"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnUpdateClick" />
        </StackPanel>

        <Button x:Name="CancelButton" Content="{loc:Loc Key=Common_Cancel}"
                CornerRadius="16" MinWidth="0" Padding="16,6" Height="32"
                HorizontalAlignment="Right" Visibility="Collapsed"
                Click="OnCancelClick" />
    </StackPanel>
</ContentDialog>