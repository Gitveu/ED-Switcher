<Window
    x:Class="EDAccountSwitcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent" Padding="12,0,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
                <Image Source="Assets/Square44x44Logo.scale-200.png" Width="16" Height="16"/>
                <TextBlock x:Name="AppTitleText" Text="ED Switcher" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <NavigationView x:Name="NavView" 
                        Grid.Row="1"
                        Loaded="NavView_Loaded" 
                        SelectionChanged="NavView_SelectionChanged"
                        PaneOpening="NavView_PaneOpening"
                        PaneClosing="NavView_PaneClosing"
                        DisplayModeChanged="NavView_DisplayModeChanged"
                        IsBackButtonVisible="Collapsed">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Overview" Tag="OverviewPage" Icon="Home" />
                <NavigationViewItem Content="Add Account" Tag="AddAccountPage" Icon="Add" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Padding="24" />
        </NavigationView>
    </Grid>
</Window>